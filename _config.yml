title: T.mkv
url: "https://tyh94.github.io"
permalink: pretty        # убирает .html из URL
plugins:
  - jekyll-sitemap

# Исключить всё лишнее из сборки
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
